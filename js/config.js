// Keep null until the receiver and completed legal information have been verified.
// No credentials belong in client-side files.
export const formConfig = Object.freeze({ endpoint: null, privacyReady: false });
